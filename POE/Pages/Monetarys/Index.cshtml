@page
@model POE.Pages.Monetarys.IndexModel

@{
    ViewData["Title"] = "Donate Money";
}

<head>
    <style>
        body {
            background-color: orange;
            aa
        }

        h1 {
            color: blue;
        }

        p {
            color: red;
        }

    </style>
</head>

<h1>Donate Money</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Monetary[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Monetary[0].Email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Monetary[0].Date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Monetary[0].Amount)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Monetary) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Amount)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
